#!/usr/bin/env bash
# DESCRIPTION: Select a Safari bookmark with fzf and open it

set -euo pipefail

script_path="$(realpath "${BASH_SOURCE[0]}")"
script_dir="$(cd "$(dirname "$script_path")" && pwd)"
exec "$script_dir/search-bookmarks" safari "$@"
