#!/usr/bin/env bash
# DESCRIPTION: Select a local branch with fzf and switch to it

set -euo pipefail

script_path="$(realpath "${BASH_SOURCE[0]}")"
script_dir="$(cd "$(dirname "$script_path")" && pwd)"
exec "$script_dir/../libexec/git/switch-branch.sh" local "$@"
