#:schema https://mise.jdx.dev/schema/mise-task.json

[check]
description = "Run repository checks on all tracked files"
run = "hk check --all --check"

[test-deployment]
description = "Test mise bootstrap deployment in isolated home directories"
run = "go test ./tests"
